{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Simple prediction service",
  "scripts": {
    "heroku-prebuild": "cd client && yarn install && yarn run build && cd ../server && yarn --production=false && yarn run build",
    "start": "cd server && nodemon lib/index.js --exec ts-node --extensions '.ts,.js' --presets es2015,stage-2"
  },
  "author": "",
  "engines": {
    "yarn": "1.5.1",
    "node": "9.7.1",
    "npm": "5.6.0"
  },
  "dependencies": {}
}
