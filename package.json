{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Simple prediction service",
  "scripts": {
    "preinstall": "echo 'preBuild' && ls && cd server && ls && yarn --production=false && yarn run build",
    "start": "node server/dist/lib/index.js",
    "postinstall": "cd client && yarn --production=false && yarn run build"
  },
  "author": "",
  "engines": {
    "yarn": "1.6.0"
  },
  "license": "MIT",
  "dependencies": {}
}
