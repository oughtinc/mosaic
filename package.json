{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Simple prediction service",
  "scripts": {
    "preinstall": "echo 'preBuild' && cd server && yarn --production=false && yarn run build",
    "start": "node server/dist/lib/index.js",
    "postinstall": "cd client && yarn --production=false && yarn run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {}
}
