{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Simple prediction service",
  "scripts": {
    "heroku-prebuild": "cd client && yarn install && yarn run build && cd ../server && yarn --production=false",
    "start": "cd server/dist && yarn run start"
  },
  "author": "",
  "engines": {
    "yarn": "1.5.1",
    "node": "9.7.1",
    "npm": "5.6.0"
  },
  "dependencies": {}
}
