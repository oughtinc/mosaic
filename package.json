{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Simple prediction service",
  "scripts": {
    "heroku-prebuild": "echo 'preBuild' && cd server && yarn --production=false && yarn run build",
    "start": "cd server && yarn run start",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "author": "",
  "engines": {
    "yarn": "1.5.1",
    "node": "9.7.0"
  },
  "dependencies": {}
}
