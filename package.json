{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Simple prediction service",
  "scripts": {
    "heroku-prebuild": "echo 'preBuild' && ls && cd server && ls && yarn && yarn install && yarn run build",
    "start": "node server/dist/lib/index.js",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "author": "",
  "engines": {
    "yarn": "1.6.0"
  },
  "license": "MIT",
  "dependencies": {}
}
